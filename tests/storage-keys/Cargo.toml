[package]
name = "storage-key-tests"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
doctest = false

[dependencies]
earnproof-shared.workspace = true
soroban-sdk.workspace = true

[dev-dependencies]
issuer-registry = { path = "../../contracts/issuer-registry", features = ["testutils"] }
proof-registry = { path = "../../contracts/proof-registry", features = ["testutils"] }
protocol-config = { path = "../../contracts/protocol-config", features = ["testutils"] }
soroban-sdk = { workspace = true, features = ["testutils"] }
